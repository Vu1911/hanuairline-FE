import React from 'react'
import PropTypes from 'prop-types'

// TODO: @des: PrivateRoute is component that only allow authenticated user to access the route
// TODO: @Todo: implement the reusable private component that switch user throughout private route
// TODO:        and redirect user to the current url if user is not a auth user
const PrivateRoute = props => {
    return (
        <div>
            
        </div>
    )
}

PrivateRoute.propTypes = {

}

export default PrivateRoute
