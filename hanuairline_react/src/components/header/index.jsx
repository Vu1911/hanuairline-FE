import React from 'react';
import './style.scss';

// TODO: build header that: take state prop store as props
// take action from store as propTypes to logout 
const Header = () => {
    return (
        <div>
            Header...
        </div>
    )
};

export default Header;
